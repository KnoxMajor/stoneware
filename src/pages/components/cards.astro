---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout title="Cards">
  <h1>Cards</h1>
  <p>Containers for grouping related content.</p>

  <h2>Default Card</h2>
  <p>Elevated with shadow for primary content.</p>
  <div class="preview" style="background: var(--sw-bg-page);">
    <div class="sw-card" style="max-width: 400px;">
      This is a basic card with default styling. It has a subtle shadow for elevation.
    </div>
  </div>
  <pre><code set:html={`&lt;div class="sw-card"&gt;
  Card content here
&lt;/div&gt;`}></code></pre>

  <h2>Bordered Card</h2>
  <p>Border instead of shadow. Use inside other cards or modals.</p>
  <div class="preview">
    <div class="sw-card sw-card-bordered" style="max-width: 400px;">
      Bordered card without shadow. Good for nested content.
    </div>
  </div>
  <pre><code set:html={`&lt;div class="sw-card sw-card-bordered"&gt;
  Bordered card without shadow. Good for nested content.
&lt;/div&gt;`}></code></pre>

  <h2>With Header</h2>
  <div class="preview" style="background: var(--sw-bg-page);">
    <div class="sw-card" style="max-width: 400px;">
      <h3 class="sw-card-title">Card Title</h3>
      <p class="sw-card-subtitle">Optional subtitle text</p>
      Card content goes here.
    </div>
  </div>
  <pre><code set:html={`&lt;div class="sw-card"&gt;
  &lt;h3 class="sw-card-title"&gt;Card Title&lt;/h3&gt;
  &lt;p class="sw-card-subtitle"&gt;Optional subtitle&lt;/p&gt;
  Card content goes here.
&lt;/div&gt;`}></code></pre>

  <h2>With Footer</h2>
  <h3>Design Notes</h3>
  <p>Constructive action goes on left</p>
  <div class="preview" style="background: var(--sw-bg-page);">
    <div class="sw-card" style="max-width: 400px;">
      <h3 class="sw-card-title">Settings</h3>
      <p class="sw-card-subtitle">Make changes to your account settings here.</p>
      <button class="sw-btn sw-btn-primary">Save</button>
      <button class="sw-btn sw-btn-secondary">Cancel</button>
    </div>
  </div>
  <pre><code set:html={`&lt;div class="sw-card"&gt;
  &lt;p&gt;Settings&lt;/p&gt;
  &lt;p class="sw-card-subtitle"&gt;Make changes to your account settings here.&lt;/p&gt;
  &lt;button class="sw-btn sw-btn-primary"&gt;Save&lt;/button&gt;
  &lt;button class="sw-btn sw-btn-secondary"&gt;Cancel&lt;/button&gt;
&lt;/div&gt;`}></code></pre>

  <h2>CSS</h2>
  <pre><code set:html={`.sw-card {
  background: var(--sw-bg-card);
  border-radius: var(--sw-radius-2xl);
  box-shadow: var(--sw-shadow-md);
  overflow: hidden;
  padding: var(--sw-space-5) var(--sw-space-6);
}

.sw-card-bordered {
  border: 1px solid var(--sw-border-default);
  box-shadow: none;
}

.sw-card-elevated {
  box-shadow: var(--sw-shadow-lg);
}

.sw-card-title {
  font-size: var(--sw-text-lg);
  font-weight: var(--sw-font-semibold);
  color: var(--sw-text-primary);
  margin: 0;
}

.sw-card-subtitle {
  font-size: var(--sw-text-sm);
  color: var(--sw-text-secondary);
  margin: var(--sw-space-1) 0 0 0;
}`}></code></pre>
</BaseLayout>
